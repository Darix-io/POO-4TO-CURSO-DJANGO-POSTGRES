# Tarea 2 - Proyecto de Django

## Información del estudiante
- Nombre completo del estudiante: **[RUBEN DARIO MORA JIMENEZ**
- Carrera y semestre: **[POO - 4TO SEMESTRE]**

## Resumen técnico de pasos realizados
1. Se revisó el repositorio Git local y se detectó que la carpeta `backend` estaba registrada como un submódulo en el índice de Git.
2. Se corrigió el estado del repositorio eliminando el enlace de submódulo y re-agregando `backend` como un directorio normal con sus archivos de código.
3. Se actualizó `.gitignore` para evitar subir archivos de configuración y backups no deseados.
4. Se realizó un commit local con el mensaje `Fix backend submodule and track backend as normal directory`.
5. El repositorio quedó listo para agregar un remoto GitHub y hacer `git push` sin destruir el proyecto.

